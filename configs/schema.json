{
    "properties": {
        "shopName":    {
            "type":    "string",
            "options": {
                "label":   "Shop Name",
                "required": true
            }
        },
        "appVersion": {
            "type": "string",
            "options": {
                "label": "App Version",
                "required": true,
                "description": "The version of your app. Incrementing these will trigger update events next login.",
                "default": "0.0.1"
            }
        },
        "apiKey":       {
            "type":    "string",
            "options": {
                "label":       "API Key",
                "required":     true
            }
        },
        "sharedSecret": {
            "type":    "string",
            "options": {
                "label":       "Shared Secret",
                "required":    true
            }
        },
        "scope": {
            "type":    "string",
            "options": {
                "label":       "Scope",
                "default":     "read_products"
            }
        },
        "redirectUrl": {
            "type":    "string",
            "options": {
                "label":       "Redirect URL",
                "description":     "Where to send someone after authenticating."
            }
        },

        "privateAppKey": {
            "type": "string",
            "options": {
                "label": "Private App Key",
                "description": "Create a new private app and enter the key here and connections will always work to your shop."
            }
        },

        "privateAppPassword": {
            "type": "password",
            "options": {
                "label": "Private App Password",
                "description": "Create a new private app and enter the password here and connections will always work to your shop."
            }
        },

        "preferencesSchema": {
            "type":    "object",
            "options": {
                "label":       "Preferences Schema",
                "description":     "How the preferences form will render for a shop."
            }
        },

        "localAddress": {
            "type":    "string",
            "options": {
                "label":       "Local Address",
                "description": "Change the IP address requests to Shopify originate from."
            }
        },
        "nonce": {
            "type": "string",
            "options": {
                "label": "Nonce",
                "description": "For the state check passed back and forth with shopify."
            }
        }
    }
}